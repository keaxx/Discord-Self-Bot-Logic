{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "playwright install-deps && playwright install --with-deps chromium && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "restartPolicyType": "always",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 180
  }
}
